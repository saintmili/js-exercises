let first_name = 'Mili';
let age = 23;
let data = `I am ${first_name} and ${age} year's old.`;
console.log(data);

// methods
/*
    data.length()
    data.slice(0, 10)
    data.substr(0, 3)
    data.includes('old')
    data.toLowerCase()
    data.toUpperCase()
    data.search('and')
    data.replace('I am', `I'm`)
    data.trim()
    data.startsWith('Y')
    data.endsWith('.')

    age.padStart(4, 0)
    age.padEnd(4, 0)
*/